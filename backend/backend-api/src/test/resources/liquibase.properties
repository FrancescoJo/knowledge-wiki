# Classpath test configuration for Liquibase.
# The testFixtures dependency pulls in backend-infrastructure's main JAR alongside
# its classes+resources directory, causing Liquibase to find the changelog in two
# locations. WARN mode picks the first occurrence (both are identical).
duplicateFileMode=WARN
